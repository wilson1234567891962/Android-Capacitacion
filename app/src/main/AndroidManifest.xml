<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.co.bicicletas">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bicicletas"
        android:usesCleartextTraffic="true">

        <activity android:name=".view.activities.HomeActivity">

        </activity>

        <activity android:name=".view.activities.SplashActivity"
            android:theme="@style/Theme.Bicicletas.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activities.forgetPass"
            android:theme="@style/Theme.Bicicletas.NoActionBar"></activity>
        <activity android:name=".view.activities.MainActivity"
            android:theme="@style/Theme.Bicicletas.NoActionBar"></activity>
    </application>

</manifest>